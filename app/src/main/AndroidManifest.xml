<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eresapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".setAppointments"/>
        <activity android:name=".activities.MentorActivity" />
        <activity android:name=".activities.ViewComplaints" />
        <activity android:name=".activities.UpdateEventsCalender" />
        <activity android:name=".activities.ResManagerActivity" />
        <activity android:name=".activities.HCActivity" />
        <activity android:name=".activities.CareTakerActivity" />
        <activity android:name=".activities.StudentActivity" />
        <activity android:name=".activities.WelcomeScreen" />
        <activity android:name=".activities.BrowseAnnouncements" />
        <activity android:name=".activities.Register" />
        <activity android:name=".activities.PostAnnouncements" />
        <activity android:name=".activities.ManageProfile" />
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>